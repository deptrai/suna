# Chain Lens Startup Script Updates v3.1

## 🎯 Tổng quan thay đổi

Script `start_dev_v3_enhanced.sh` đã được cập nhật để tích hợp **Optimized Monitoring Dashboard v3.0** với Backend/Frontend logs maximized.

## 📝 Files được cập nhật

### 1. `scripts/core/start_dev_v3_enhanced.sh` → v3.1
- ✅ Cập nhật header và version từ v3.0 → v3.1
- ✅ Thêm tính năng "Optimized Dashboard Layout" vào enhanced features list
- ✅ Sửa đổi dashboard launch command: `start_optimized_monitor.sh`
- ✅ Cập nhật management commands để sử dụng `dashboard_manager.sh`
- ✅ Thay đổi mô tả monitoring dashboard trong summary

### 2. `scripts/dashboard_manager.sh` → NEW
- ✅ Utility script mới để quản lý optimized monitoring dashboard
- ✅ Commands: start, attach, detach, status, stop, restart, logs, help
- ✅ Real-time service status checking
- ✅ Session management và error handling

### 3. Existing monitoring scripts (unchanged):
- ✅ `scripts/start_optimized_monitor.sh` - Quick launcher
- ✅ `scripts/monitoring/enhanced_dashboard_monitor.sh` - Main dashboard
- ✅ `scripts/monitoring/OPTIMIZED_LAYOUT.md` - Documentation

## 🚀 Cách sử dụng mới

### Option 1: Enhanced startup (Recommended)
```bash
# Khởi động toàn bộ development environment với optimized dashboard
./scripts/core/start_dev_v3_enhanced.sh
```

### Option 2: Dashboard manager
```bash
# Quản lý dashboard độc lập
./scripts/dashboard_manager.sh start    # Launch dashboard
./scripts/dashboard_manager.sh status   # Check status  
./scripts/dashboard_manager.sh attach   # Attach to session
./scripts/dashboard_manager.sh stop     # Stop dashboard
./scripts/dashboard_manager.sh restart  # Restart dashboard
```

### Option 3: Quick launcher
```bash
# Chỉ khởi động dashboard (services phải đã chạy)
./scripts/start_optimized_monitor.sh
```

## 🎮 Dashboard Layout

```
┌─────────────────┬─────────────────┐ ← LARGE (75% height)
│ 🔧 Backend Logs │ 🌐 Frontend Logs│   [PRIMARY PANES]  
│    [PRIMARY]    │    [PRIMARY]    │   Maximum readability
├─────────────────┼─────────────────┤ ← small (25% height)
│ 🔄 Redis Monitor│ ⚙️ Worker Logs   │   [SECONDARY PANES]
└─────────────────┴─────────────────┘   Monitoring stats
```

## ✨ Tính năng mới

1. **Backend/Frontend Maximized**: 75% screen space cho development logs
2. **Dashboard Manager**: Centralized utility để manage monitoring session
3. **Enhanced Commands**: Streamlined management commands trong startup summary
4. **Service Status Integration**: Real-time service status checking
5. **Backward Compatibility**: Các scripts cũ vẫn hoạt động bình thường

## 🔧 Technical Changes

### Enhanced Features List (updated):
```bash
echo -e "   🗺️ Optimized Dashboard Layout (Backend/Frontend maximized)"
```

### Dashboard Launch Command (updated):
```bash
exec "$SCRIPT_DIR/start_optimized_monitor.sh"
```

### Management Commands (updated):
```bash
echo -e "   Dashboard Manager:   ${CYAN}./dashboard_manager.sh [start|attach|status|stop|restart]${NC}"
echo -e "   Quick start:         ${CYAN}./start_optimized_monitor.sh${NC}"
echo -e "   Dashboard status:    ${CYAN}./dashboard_manager.sh status${NC}"
```

## 📋 Migration Notes

- **No Breaking Changes**: Existing workflows vẫn hoạt động
- **Enhanced Experience**: Developer experience được cải thiện với larger log panes
- **New Utilities**: Dashboard manager cung cấp better control over monitoring session
- **Consistent Commands**: Tất cả commands được standardize và document rõ ràng

---

*Generated by Chain Lens Development Team - Optimized for Backend/Frontend Development Experience*