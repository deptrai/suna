import { test, expect } from '@playwright/test';

test.describe('Generative Widgets E2E', () => {
  test('[P0] should render token info card', async ({ page }) => {
    await page.goto('/');
    // E2E UI flow
  });
});
